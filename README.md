# dropbox
dropbox-backend

  Please follow the following step to run backend
  1. git clone <repo>
  2. mvn clean install
  3. mvn spring-boot:run


Notes: -
- docs will be stored in uploads folder
- H2 is used so no need of external db setup
- db will be cleared after every build


